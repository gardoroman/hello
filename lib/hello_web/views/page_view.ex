defmodule HelloWeb.PageView do
  use HelloWeb, :view

  # overrides the template in page/index.html.eex
  # def render("index.html", assigns) do
  #   "rendering with assigns #{inspect Map.keys(assigns)}"
  # end
end
